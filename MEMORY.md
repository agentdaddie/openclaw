# MEMORY.md

Long-term notes for this workspace.

## Preferences
- Keep Telegram channel enabled for this workspace.
- Keep provider/model configuration in `.env` + docker-compose.

## Decisions
- Port mapping uses host `8888` to container `8080`.
- Custom config is mounted from `/app/config/openclaw.json`.

## Notes
- Use `memory/YYYY-MM-DD.md` for daily logs.
- Keep this file concise and durable.
